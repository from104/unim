# Changelog

<!-- markdownlint-disable MD024 -->

UNIM(Universal Next-generation Input Method) 프로젝트에 대한 모든 주목할만한 변경 사항은 이 파일에 기록됩니다.

형식은 [Keep a Changelog (korean)]를 기반으로 하며 이 프로젝트는 [Semantic Versioning (korean)]을 따릅니다.

## [Unreleased]

### 수정됨

- Windows: 무인 설치(`/qn`, winget)에서 설치기가 팝업 렌더러를 띄우지 않도록 변경 (winget 자동 검증 통과 요건, 설치 스크립트는 종전대로 직접 실행)

## [0.4.2] 2026-09-09

### 추가됨

- 여섯 배포판(우분투 24.04·26.04, Debian 13, 페도라 43·44, RHEL 10 계열)별 릴리스 빌드 제공, 설치 스크립트 자동 감지·설치 지원 (RHEL 10 계열 신규 지원, EPEL 저장소 필요)
- 빌드 시 GNOME 확장이 새 셸 버전에서도 정상 동작하는지 정적·동적 검증 추가 (`make check-compat`)
- 배포판별 빌드에 설치 후 IM 모듈 실제 로드·키 입력 동작 검증 추가 (GTK3/4·Qt5/6·XIM·Windows MSI)
- Windows 배포 산출물의 백신 오탐(트로이목마 오인) 방지 — 파일 버전 정보 삽입, CI Defender 사전 검사, 코드 서명 파이프라인 도입
- 다음 우분투 LTS 컨테이너에서 CI 사전 빌드·테스트 지원

### 달라진 점

- `unim-cli config user-dict`·`config blacklist` 를 최상위 `unim-cli dict user-dict`·`dict blacklist` 로 이동 (옛 경로는 안내 메시지와 함께 당분간 동작)
- `unim-cli dict` 의 단어 추가·제거에 여러 단어를 한 번에 지정 가능, 여러 개 제거 시 확인 질문 (`-y` 로 생략)

### 수정됨

- GNOME Shell 50(우분투 26.04)에서 한글 입력이 안 되던 문제 수정
- 샌드박스 앱(Flatpak·Snap)에서 조합 중인 한글이 보이지 않던 문제 수정
- GTK `im-xim` 폴백 환경에서 조합이 멈추던 문제 수정, 조합 중 글자를 커서 옆 별도 창에 표시
- 설정 저장 순간 조합 중이던 글자가 사라지거나 조합이 끊기던 문제 수정
- XIM 서버가 앱 종료 시 함께 죽어 이후 앱의 XIM 입력이 막히던 문제 수정
- Windows: 자동 영문 전환 중 `Ctrl`+`B` 같은 합성키가 동작하지 않던 문제 수정
- Windows: 갱신 후 트레이 메뉴가 안 보일 때 탐색기 재시작으로 즉시 복구하는 버튼 추가
- Windows: 크롬 등에서 편집 칸에 포커스가 없을 때 문자키가 브라우저 단축키로 전달되지 않던 문제 수정

### 알려진 문제

- 조합 도중 누른 수정자 조합이 래치를 유지하지 못하는 현상 — GNOME 50(우분투 26.04)부터 해소, 이전 버전은 잔존
- Windows: 설치 직후 재로그인 전까지 트레이 메뉴 미표시 가능

## [0.4.1] 2026-08-16

### 수정됨

- rpm 패키지 재게시 — jemalloc 빌드 스크립트의 `MAKEFLAGS` 처리 오류, Qt 6.8 이상 `GuiPrivate` 미명시 문제 수정, 페도라 43 빌드 확인
- GNOME Wayland 고정키(Sticky Keys) 조합(`Ctrl`→`A` 등)이 간헐적으로 풀리던 문제 수정, `Shift`+`Home` 등 선택 조작도 함께 수정 (GNOME 확장 코드라 적용하려면 로그아웃 후 재로그인 필요)

### 알려진 문제

- 조합 도중 누른 수정자 조합이 래치를 유지하지 못함, 조합 중이던 글자가 먼저 커밋됨
- 샌드박스 앱(Flatpak·Snap)에서 조합 중인 한글 미표시
- GTK `im-xim` 환경에서 조합 멈춤
- Windows: 자동 영문 전환 중 `Ctrl`+`B` 같은 합성키 미동작
- Windows: 새로 설치·갱신 후 트레이 우클릭 메뉴 미표시

## [0.4.0] 2026-08-10

Windows 지원(리눅스와 같은 코어)·한 줄 설치·두 플랫폼 공통 설정 창 도입 릴리스

2026-07-19 게시분 v0.4.0 태그는 MSI 릴리스 게이트 오류(`guids.wxi` 버전 불일치, `65c66f8`에서 수정)로 회수, 이 항목이 유효한 v0.4.0

### 추가됨

- 한 줄 설치 지원 (리눅스 `curl … | bash`, Windows `irm … | iex`) — 배포판 자동 감지·SHA256 검증, `UNIM_VERSION`으로 버전 고정 가능
- 설치 후 첫 로그인 시 기본 입력기 지정 마법사 자동 실행 (최초 1회)
- 설정 앱을 GTK4에서 Slint로 재작성, 리눅스·Windows 동일 화면 제공
- 한글 조합 확정 단위를 음절 대신 단어로 선택 가능 (터미널·XIM·모아치기 자판은 자동으로 음절 단위 유지)
- AutoTypeFix 전체/순방향/역방향 개별 토글 단축키 추가 (전체 토글 기본값 `Shift+F8`)
- 비밀번호 입력란에서 자동 영문 전환 및 키 입력 기록(버퍼·실행취소·학습 사전) 차단 지원
- 자동 영문 전환 트리거로 `Ctrl+B` 같은 수정자 조합 지원 (트리거 키는 그대로 앱에 전달)
- 한/영 모드 전환 시 알림음 재생 기능 추가 (설정에서 끄기 가능)
- 한/영 토글 키·한글 문자 키의 자동 반복(길게 누름) 무시 옵션 추가 (기본값 꺼짐)
- 자판 편집 도구 자판 스튜디오와 타자 연습 도구 추가 (리눅스)
- 인디케이터·설정·자판 스튜디오·타자 연습 전용 아이콘 및 앱 ID 정리, Windows 실행 파일 아이콘 적용

### 달라진 점

- 설정 앱 이원화 — Slint 신규 앱이 `unim-settings` 이름을 승계, 기존 GTK4 앱은 `unim-settings-gtk`로 개명해 병행 배포
- deb 패키지 11종으로 개편 — `unim-settings`·자판 스튜디오·타자 연습을 신규 패키지로 분리, 인디케이터·팝업·레거시 GTK 다이얼로그는 `unim-desktop`으로 통합
- 자판 스튜디오 UI 재설계 — 좌측 사이드바 2탭 구성을 헤더 드롭다운(언어 › 출처 › 자판) + 4탭 구성으로 변경
- 자판 목록을 등록된 프로필 기준으로 표시, 안마태·사용자 자판 이름 노출 및 모아치기 UI 자동 활성화
- GNOME 확장 트레이·패널 아이콘을 단색 SVG 세트로 교체, 비활성 상태 전용 아이콘 추가
- 잘못된 단축키 표기 저장 시 경고를 AutoTypeFix 토글 항목에서 한/영 전환·한자·자동 영문 트리거 키까지 확대, 전체 무효 시 저장 거부

### 수정됨

- 도움말이 기본 브라우저 대신 IDE로 열리던 문제 수정 (text/html 기본 핸들러 우회)
- 오른쪽 Alt 한/영 전환이 동작하지 않던 문제 수정, 판정을 데몬으로 일원화
- GTK/Qt 입력 모듈에서 `Super` 조합(예: `Super+X`)이 인식되지 않던 문제 수정
- 순수 Wayland 환경(Sway·단독 Hyprland 등)에서 모든 키가 잘못 인식되던 문제 수정
- GNOME Wayland에서 비밀번호 필드 자동 보호가 동작하지 않던 문제 수정
- XIM 앱에서 음절 확정 직후 다음 글자가 한 박자 늦게 보이던 문제 수정
- 조합 중 입력란의 다른 위치를 클릭하면 글자가 클릭 지점에 확정되던 문제 수정 (GNOME Wayland·XIM·Qt)
- XIM 앱에서 조합 중 Enter 입력 시 줄바꿈이 글자보다 먼저 반영되던 문제 수정
- 단축키 입력란의 잘못된 예시(`ScrollLock`·`Hangul`) 안내를 유효한 표기(`F10`·`Korean`·`Hanja`)로 수정
- 단어 단위 입력이 터미널 등에서 음절 단위로 자동 전환될 때의 원인을 로그와 설정 설명에 명시

### Windows

- 언어바·조합/후보 팝업·AutoTypeFix UI를 `unim_tsf.dll` 하나로 통합, 별도 도우미 실행 파일 제거
- WezTerm·텔레그램 등 인라인 조합 앱에서 한글 조합 복구 (CUAS 계약 준수)
- 32비트 TSF(`unim_tsf32.dll`) 등록으로 카카오톡·한컴 등 32비트 앱 지원, 불필요한 IMM32 `.ime` 등록 폐기
- 조합·후보 창 TSF UIA/UILess 노출, 자동 반복 억제 옵션·한/영 전환 화면 낭독기 통지 추가
- WiX 3.x 기반 MSI 빌드 체인 정비 (deb·rpm보다 몇 분 늦게 첨부될 수 있음)

---

### 알려진 문제

- 이 릴리스에는 rpm 패키지 미포함, 페도라·RHEL 계열 한 줄 설치 불가 ([0.4.1](https://github.com/from104/unim/releases/tag/v0.4.1)에서 해결)

## [0.3.0] 2026-05-19

모아치기(동시 자모 입력)·팝업 마우스 조작 전면 개편·즐겨찾기(★)·단일 통합 설정 창·첫 빌트인 모아치기 자판 안마태 도입 릴리스

### 추가됨

- 빌트인 한손 모아치기 세벌식 자판 안마태(Ahnmatae 2003) 추가
- 모아치기(동시 자모 입력) 엔진 v4 추가, 설정 → 자판에서 동시 입력 시간(ms) 슬라이더 조정 가능 (기본 60ms, 0이면 비활성)
- 자모 역순 결합 옵션 추가 (기본 꺼짐) — 초·중·종성 입력 순서에 관계없이 결합
- GNOME 환경에서 한자·특수문자·이모지 팝업 바깥 클릭으로 닫기 지원
- 한자·특수문자·이모지 팝업에 마우스 페이지 이동(◀/▶) 버튼 추가
- 한자 후보 즐겨찾기(★/☆) 기능 추가, 변환 시 즐겨찾기 후보를 목록 상단에 노출
- 한자 팝업에 9×9 확장 격자 모드 추가 (⊞/⊟ 아이콘으로 전환)
- 이모지 팝업에 세로 카테고리 탭과 단축키(A/S/D/F 등) 추가
- AutoTypeFix 오교정 학습 블랙리스트 추가, 우클릭으로 등록하고 설정 → 억제 단어에서 관리
- Fedora·openSUSE·RHEL 계열용 rpm 패키지 빌드 지원 추가

### 달라진 점

- GTK·Qt로 나뉘어 있던 설정 창을 GTK4 + libadwaita 기반 단일 설정 창(`unim-settings`)으로 통합
- 트레이 아이콘을 별도 프로세스(`unim-indicator`)로 분리, 설정 창과 독립적으로 종료·실행 가능
- 한자·특수문자·이모지 팝업 렌더링을 별도 백그라운드 서비스(`unim-popup-service`)로 분리, 첫 사용 시 자동 시작
- 설정 화면의 숫자 입력을 슬라이더 + 눈금 방식으로 변경
- 설정 화면 전 항목에 구체적인 설명·권장값 안내 추가
- 이모지 팝업 사용 여부 설정 항목 제거, 한글 비조합 중 한자 키로 항상 사용 가능
- 모아치기 동시 입력 시간 상한을 200ms로, 기본값을 60ms로 변경

### 수정됨

- XIM 환경(XTerm·WezTerm 등 OVER-THE-SPOT)에서 음절 확정 직후 다음 자모가 한 프레임 늦게 보이던 문제 수정

### 제거됨

- 빌트인 자판에서 쿼티형 세벌식(`ko_3bul_qwerty`) 제거, 필요 시 참고용 JSON을 사용자 자판으로 등록 가능
- Qt 설정 창(`unim-gui-qt`) 제거, GTK4 `unim-settings`로 단일화

### 업그레이드 안내

- 0.2.0에서 업그레이드 시 설정 파일·사용자 자판 그대로 유지, 별도 마이그레이션 불필요
- `unim-gui-qt` 사용자는 `unim-settings`·`unim-popup-service`로 전환 필요
- `ko_3bul_qwerty` 사용자는 설정 → 자판에서 자판 재선택 필요 (자동 이전 안 됨)
- 0.1.x에서 만든 v0 형식 사용자 자판 JSON은 v1 형식으로 변환 필요

### 알려진 문제

- KDE Plasma 5.x Wayland에서 한자·특수문자·이모지 팝업 미표시 (`gtk4-layer-shell` 라이브러리 부재, X11 세션 또는 GNOME 사용 권장)
- KDE Plasma 6 Wayland·Sway·Hyprland·river 등 단독 Wayland 컴포지터 검증 미비, 팝업 위치·포커스 전환 회귀 가능
- 일부 드문 XIM ON-THE-SPOT 앱에서 음절 확정 직후 다음 자모 preedit 한 프레임 누락 가능

## [0.2.0] 2026-04-26

### 추가됨

- 사용자 정의 자판 프로필(JSON) 추가, 실시간 핫리로드 지원
- 자판 프로필 상속(inherits)·룰셋 옵션 지원, 설정 화면에서 룰셋 개별 토글 가능
- `unim-cli config layout` 서브커맨드 추가 (`list`/`describe`/`validate`)
- 빌트인 자판에 쿼티형 세벌식(`ko_3bul_qwerty`) 추가
- AutoTypeFix 오교정 자동 감지 및 학습 억제(블랙리스트) 기능 추가, 설정 → 억제 단어에서 확정·비활성화·관리 가능
- 한자 팝업에 9×9 확장 격자 모드 추가 (Period 키로 전환, ⊞/⊟ 아이콘 표시)
- 한자 후보 즐겨찾기(☆/★) 기능 추가, 여러 팝업 간 실시간 동기화
- 역방향 AutoTypeFix 학습용 사용자 사전 등록 기능 추가 (선택 영역을 단축키로 등록, GUI에서 관리)
- 지정한 트리거 문자(`:`, `/` 등)에서 한글 → 영어 자동 전환 지원 추가
- 이모지 팝업(`Super`+`.`) 추가 — 카테고리 탭·검색·최근 사용 즐겨찾기 지원

### 변경됨

- 한글 자판 설정값이 고정 목록 대신 자유 문자열로 변경, 기존 `config.yaml` 값 자동 변환
- 영문 자판 설정값도 동일하게 자유 문자열로 변경, 기존 값 자동 변환
- 역방향 AutoTypeFix 오교정 억제 감지 조건 완화로 더 정확하게 동작
- 역방향 AutoTypeFix 억제 목록 저장 방식 수정으로 의도한 단어가 정상적으로 억제되도록 개선
- AutoTypeFix 블랙리스트 등록 시점을 롤백 감지 시점에서 재시도 시점으로 변경, 오탐 감소
- 설정 CLI를 `unim-cli config`로 일원화

### 고쳐짐

- 영어 모드에서 Space가 누락되던 문제 수정 (gedit 등)
- 입력란 포커스 아웃 시 글자가 중복 커밋되던 문제 수정 (예: "늘"이 두 번 입력)
- AutoTypeFix 억제 유효기간 단위를 일에서 시간으로 변경 (1~12시간)
- gedit·gnome-text-editor에서도 역방향 AutoTypeFix 동작하도록 지원 추가
- GTK3/4 앱(터미널 포함)에서 조합 종료 시 키 입력이 잠기던 문제 수정
- XIM 앱에서 여러 글자 AutoTypeFix 교정이 정상 동작하도록 재구현 (Chrome 조합 중 예외 케이스 잔존)

## [0.1.0] 2026-04-21 — 첫 정식 릴리스

UNIM(Universal Next-generation Input Method) 첫 정식 릴리스 — 한국어 입력기 엔진을 처음부터 Rust로 재설계, 컴포넌트 구성은 아래 참조

### 추가됨 — 엔진 코어

- 순수 Rust 한글 엔진 추가 (2벌식·3벌식 390/391 조합·분해, UI·플랫폼 의존성 없음)
- DBus 데몬 아키텍처 추가 (`unim-daemon`+`unim-dbus`, 서비스명 `org.atit.unim.InputMethod`)
- C API 래퍼(`unim-capi`/`libunim_capi`) 추가로 C/C++ 프런트엔드 지원
- 통합 CLI(`unim-cli`) 추가 (한↔영 변환 및 `config` 서브커맨드)

### 추가됨 — 프런트엔드

- GTK3/GTK4 입력기 모듈 추가
- Qt5/Qt6 플랫폼 입력 컨텍스트 플러그인 추가
- XIM 프런트엔드 추가 (Over-The-Spot Preedit 지원, X11R7.6 XIM 명세 적합성 검증)
- Wayland 프런트엔드 추가 (`input-method-v2`+`virtual-keyboard-v1`, KDE Plasma 기초 지원, 한자/특수문자 팝업 통합)
- GNOME Shell 확장 추가 (자판 전환 단축키, 터미널 인식 붙여넣기 모드 등)

### 추가됨 — GUI

- GTK4/libadwaita 설정 다이얼로그(`unim-gui-gtk`) 추가 (트레이 아이콘, 한자/특수문자 팝업 포함)
- Qt6 대체 설정 GUI(`unim-gui-qt`) 추가
- 시스템 im-config 도구 연동 지원 추가

### 추가됨 — 기능

- 한글 자판 2벌식·3벌식(390/391/노시프트) 빌트인 지원
- AutoTypeFix(자동 한↔영 오타 교정) 순방향·역방향 지원 (XIM/GTK/Qt/GNOME)
- 한자 변환 팝업 추가 (검색·페이지 이동·색인 키 이동)
- 특수문자·이모지 검색 팝업 추가
- 앱별 입력 모드 자동 전환 규칙 추가

### 추가됨 — 패키징 및 문서

- Debian 패키지 9종으로 분리 배포 (`unim-common`·`unim-im-gtk`·`unim-im-qt`·`unim-xim`·`unim-wayland`·`unim-gui-gtk`·`unim-gui-qt`·`unim-gnome`·전체 스택 메타패키지 `unim`)
- 컴포넌트별 `SPEC.md` 12종과 `IME_BEHAVIOR.md`·`POPUP_SPEC.md` 등 종합 문서 제공

[Keep a Changelog (korean)]: https://keepachangelog.com/ko/1.0.0/
[Semantic Versioning (korean)]: https://semver.org/lang/ko/
