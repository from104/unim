#!/bin/sh
set -e

case "$1" in
    remove|upgrade|deconfigure)
        # Stop all user instances of unim-daemon
        pkill -u "$(logname 2>/dev/null || echo root)" -x unim-daemon 2>/dev/null || true
        ;;
esac

#DEBHELPER#

exit 0
