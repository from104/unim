# unim-gui 제품 요구사항 명세서 (PRD)

## 1. 문서 개요

- **문서 버전**: 1.0
- **작성일**: 2024-07-30
- **작성자**: from104 ( Gemini )

---

## 2. 제품 소개

- **제품명**: 유님 그놈 확장 (unim-gnome-extension)
- **제품 요약**: 그놈(GNOME) 데스크톱 환경을 위한 확장 프로그램으로, 사용자가 한글을 입력해야 할 때 영문으로 입력하거나, 영문을 입력해야 할 때 한글로 입력하는 실수를 자동으로 감지하고 올바른 텍스트로 변환해주는 유틸리티입니다.
- **문제 해결**:
  - 현재 입력기 상태를 확인하지 않고 타이핑하여 발생하는 한/영 오타 문제를 해결합니다.
  - 'dkssudgktpdy'와 같이 잘못 입력된 영문자를 '안녕하세요'와 같은 한글로 실시간으로 변환합니다.
  - '한글'을 입력해야 할 때 'gksrmf'로 잘못 입력된 경우, 이를 감지하고 수정합니다.
  - 텍스트 변환과 동시에 그놈 데스크톱의 입력기 상태를 자동으로 동기화하여 사용자의 추가적인 키보드 조작 없이도 원활한 타이핑 경험을 제공하는 것을 목표로 합니다.

---

## 3. 대상 사용자

- 그놈 데스크톱 환경을 사용하는 모든 한국어 사용자.
- 개발자, 작가, 학생 등 한글과 영문을 자주 번갈아 가며 입력하는 사용자.

---

## 4. 제품 목표 및 핵심 기능

### 4.1. 주요 목표

- **자동 변환 및 교정**: 사용자의 키 입력을 분석하여 한/영 오타를 실시간으로 감지하고, 올바른 언어의 텍스트로 자동 변환합니다.
- **입력기 상태 동기화**: 텍스트 변환 시, 그놈 시스템의 현재 입력 소스(Input Source)를 변환된 언어에 맞게 자동으로 전환합니다.
- **높은 안정성**: 최신 그놈 버전을 포함한 다양한 그놈 데스크톱 환경에서 안정적으로 동작하도록 합니다.
- **직관적인 설정**: 사용자가 확장의 핵심 기능을 쉽게 제어할 수 있는 설정 UI를 제공합니다.

### 4.2. 핵심 기능

- **실시간 오타 감지**:
  - 사용자의 키 스트로크를 실시간으로 버퍼에 저장하고 분석하여 한/영 오타 패턴을 식별합니다.
  - `unim` 라이브러리의 핵심 로직을 사용하여 변환의 정확도를 높입니다.

- **자동 텍스트 변환**:
  - 스페이스바, 엔터 키 입력 등 단어 입력이 끝나는 시점에 버퍼에 쌓인 키 스트로크를 분석하여 변환을 수행합니다.
  - 변환된 텍스트는 기존에 잘못 입력된 텍스트를 대치합니다.

- **입력 소스 자동 전환**:
  - 텍스트가 한글에서 영문으로, 또는 영문에서 한글로 변환될 때 그놈의 입력 소스를 자동으로 해당 언어로 변경합니다.

- **수동 변환 기능**:
  - 사용자가 이미 입력된 텍스트 영역을 선택한 후, 지정된 단축키를 눌러 수동으로 한/영 변환을 실행할 수 있는 기능을 제공합니다.

- **다양한 키보드 레이아웃 지원**:
  - **영문**: QWERTY, Dvorak
  - **한글**: 2벌식 표준, 3벌식 390, 3벌식 391

- **설정 UI 제공**:
  - 그놈 '확장 프로그램(Extensions)' 관리자에 통합된 설정 패널을 제공합니다.
  - 제공 기능:
    - 확장 기능 전체 On/Off 토글
    - 자동 변환 기능 On/Off 토글
    - 수동 변환 기능 On/Off 및 단축키 설정
    - **사용자 키보드 레이아웃 선택 (한글/영문)**
    - 변환 시 알림 메시지 표시 여부 설정

---

## 5. 기술 요구사항

- **플랫폼**: 그놈 셸 확장 (GNOME Shell Extension)
- **대상 그놈 버전**: GNOME 42 이상
- **개발 언어**:
  - **코어 로직 (한/영 변환 엔진)**: **Rust**
    - 기존 `unim` 프로젝트의 `unim-cli.rs` 및 `src/hangul/`, `src/keystroke/` 내의 변환 로직을 라이브러리 형태로 재사용하거나 확장합니다.
  - **그놈 확장 인터페이스**: **JavaScript (GJS)**
    - Rust로 작성된 코어 로직을 Native-FFI 등을 통해 호출하여 그놈 셸과 연동하는 브릿지 역할을 수행합니다.
  - **100% Rust 개발 가능성 탐색**:
    - D-Bus 라이브러리(예: `zbus`) 등을 활용하여 Rust만으로 그놈 셸의 입력기 상태를 제어하고 키 입력을 감지하는 방식의 구현 가능성을 우선적으로 검토합니다. 이것이 가능하다면 100% Rust로 작성하는 것을 지향합니다.

- **의존성**:
  - `unim` Rust 라이브러리 (crate)
  - 그놈 셸 및 관련 개발 라이브러리 (GJS, D-Bus API 등)
  - `cargo` 및 Rust 컴파일러

---

## 6. 디자인 및 UX (사용자 경험)

- **투명한 경험 (Seamless Experience)**: 사용자가 확장 프로그램의 동작을 거의 인지하지 못할 정도로 자연스러운 변환 경험을 제공하는 것을 최우선으로 합니다.
- **최소한의 UI**: 설정 화면은 그놈 디자인 가이드라인을 준수하여 간결하고 직관적으로 설계합니다.
- **적절한 피드백**: 텍스트 변환이 실행되었을 때, 사용자에게 짧은 시각적 피드백(예: 작은 알림창)을 제공하여 상황을 인지할 수 있도록 돕습니다. (해당 기능은 설정에서 On/Off 가능)

---

## 7. 가정 및 제약사항

- **가정**:
  - 사용자는 그놈 데스크톱 환경을 사용하고 있습니다.
- **제약사항**:
  - 그놈 셸의 보안 정책 및 API 제약으로 인해 키 입력 감지 및 입력기 제어에 어려움이 있을 수 있습니다. (Wayland 환경 등)

---

## 8. 범위 외 (Out of Scope)

- **초기 버전 제외 사항**:
  - 그놈 외의 데스크톱 환경(예: KDE, XFCE, Windows, macOS)에 대한 지원.
  - 단순 한/영 변환을 넘어서는 복잡한 자연어 처리(NLP) 기능.
