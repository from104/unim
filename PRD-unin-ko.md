# unim (Unicode Input) - 한/영 자판 변환기

## 1. 프로젝트 개요

### 1.1 목적

unim은 영문 자판 상태에서 입력된 한글 타이핑을 실제 한글로 변환해주는 명령행 도구입니다. 터미널 환경에서 한글 입력 시스템을 사용할 수 없는 상황에서도 효율적인 한글 입력을 가능하게 합니다.

### 1.2 주요 기능

- 영문 자판으로 입력된 한글 문자열을 실제 한글로 변환
- 두벌식 및 세벌식(390) 자판 지원
- 파일 입력/출력 및 표준 입출력 지원
- 다양한 커맨드라인 옵션 제공

### 1.3 대상 사용자

- 리눅스/유닉스 환경의 터미널 사용자
- 서버 관리자 및 개발자
- CLI 환경에서 한글 입력이 필요한 사용자

## 2. 기술 요구사항

### 2.1 개발 환경

- 프로그래밍 언어: Rust
- 빌드 시스템: Cargo
- 타겟 플랫폼: Linux, Unix-like 시스템

### 2.2 핵심 컴포넌트

- 자체 한글 조합 라이브러리
  - 초성, 중성, 종성 분리 및 조합
  - 유니코드 한글 블록 처리
  - 두벌식/세벌식 매핑 테이블

### 2.3 의존성

- 최소한의 외부 의존성 사용
- 표준 라이브러리 활용 극대화

## 3. 기능 요구사항

### 3.1 입력 처리

- 표준 입력(STDIN)을 통한 실시간 변환
- 파일 입력 지원
  - 텍스트 파일 읽기
  - 대용량 파일 처리를 위한 스트리밍 지원
- 입력 인코딩: UTF-8

### 3.2 출력 처리

- 표준 출력(STDOUT)으로 변환 결과 출력
- 파일 출력 지원 (-o 옵션)
- 출력 인코딩: UTF-8

### 3.3 커맨드라인 옵션

- `-h, --help`: 도움말 표시
- `-k, --korean`: 영문 타자를 한글로 (기본갑)
- `-e, --english`: 한글 타자를 영문으로
- `-2, --2bulsik`: 두벌식 모드 (기본값)
- `-3, --3bulsik`: 세벌식 모드 (390)
- `-o, --output <FILE>`: 출력 파일 지정

### 3.4 동작 모드

1. 인자 없이 실행

   ```bash
   $ unim
   dkssudgktpdy
   안녕하세요
   ```

2. 파일 입력

   ```bash
   unim input.txt
   unim input.txt -o output.txt
   ```

## 4. 성능 요구사항

### 4.1 응답 시간

- 실시간 입력 시 지연 없는 변환 (100ms 이내)
- 파일 처리 시 효율적인 처리 속도

### 4.2 리소스 사용

- 메모리 사용량 최적화
- CPU 사용량 최소화

### 4.3 확장성

- 대용량 파일 처리 지원
- 스트리밍 방식의 데이터 처리

## 5. 품질 요구사항

### 5.1 안정성

- 견고한 에러 처리
- 예외 상황에 대한 적절한 피드백
- 메모리 누수 방지

### 5.2 사용성

- 직관적인 커맨드라인 인터페이스
- 명확한 에러 메시지
- 상세한 도움말 제공

### 5.3 유지보수성

- 모듈화된 코드 구조
- 명확한 문서화
- 테스트 자동화

## 6. 테스트 요구사항

### 6.1 단위 테스트

- 한글 조합 로직 테스트
- 입출력 처리 테스트
- 옵션 처리 테스트

### 6.2 통합 테스트

- 전체 변환 프로세스 테스트
- 파일 입출력 테스트
- 다양한 입력 케이스 검증

### 6.3 성능 테스트

- 대용량 파일 처리 테스트
- 메모리 사용량 모니터링
- 응답 시간 측정

## 7. 제약사항

### 7.1 기술적 제약

- Rust 안정 버전 사용
- POSIX 호환 시스템 지원
- UTF-8 인코딩만 지원

### 7.2 라이선스

- MIT 라이선스
- 오픈 소스 컴포넌트 사용 시 라이선스 호환성 확인

## 8. 배포 요구사항

### 8.1 패키징

- Cargo 패키지 제공
- 바이너리 릴리스 제공
- 설치 문서 제공

### 8.2 문서화

- README.md
- API 문서
- 사용자 가이드
