[package]
name = "unim"
version = "0.1.0"
edition = "2021"

[dependencies]
once_cell = "1.19"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
dirs = "5.0"
x11 = "2.18.2"
libc = "0.2"

[lib]
name = "unim"
path = "src/lib.rs"

[workspace]
members = [
    "unim-cli",
    "unim-capi",
    "unim-config",
    "unim-frontends/xim",
    "unim-frontends/wayland",
    "unim-daemon",
    "unim-indicator",
    "unim-dbus",
]
resolver = "2"
